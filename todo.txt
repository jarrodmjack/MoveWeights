When added set to exercise, do optimistic render with newly added set. If failure to add, refresh page.

Things that need to be added/fixed:

Do not allow negative numbers in reps/weight inputs
forgot password functionality